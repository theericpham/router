Conventions used in this refactor:
variable_name
StructName
CONSTANT
functionName(..

* Removed all sr_ from EVERYTHING. There is no more sr_ except in #includes.
* Renamed all functions and structs according to above conventions
* Changed struct sr_packet to RawFrame
* Changed arpreq_destroy to destroyArpRequest
* Camel-cased arpcache -> arpCache, arpreq -> arpRequest
* Replaced almost all abbreviations:
addr -> address, ether, eth -> ethernet, auth -> authentication,
if -> interface, rt, rtable -> routing table, wrap -> wrapper,
hrd, hw -> hardware, sz -> size, cksum -> checksum